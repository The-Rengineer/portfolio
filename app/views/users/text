<div class="container max-w-full">
  <div class="form users-form">
    <div class="form-body flex justify-center">
      <%= form_tag("/users/login") do %>
        <p class="mt-3">メールアドレス</p>
        <input name="email" class="border-2 rounded-sm border-blue-400">
        <p class="mt-3">パスワード</p>
        <!-- 以下のinputタグにtype属性を指定してください -->
        <input type="password" name="password" class="border-2 rounded-sm border-blue-400">
        <div class="flex justify-center mt-3">  
          <button class="rounded-full border-2 rounded-sm border-blue-400 block">  
            <input type="submit" value="ログイン">
          </button>
        </div>
      <% end %>
    </div>
  </div>
</div>
<div>
aaaaaaaaaaaaaaaaaaaaaaaaaaazs
</div>