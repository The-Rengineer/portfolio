<div class="main posts-new">
  <p><%= "ルーム#{@room.id}"%></p>
  <%# <%= form_tag("/chat_messages/create/#{@room.id}") do %>
    <div class="form">
      <div class="form-body">
      <form>
        <input type="text" id="content" data-behavior="room_speaker_keypress">
        <input type="submit" id ="save" data-behavior="room_speaker_click" value="保存">
      </form>
      </div>
    </div>
  <%# <% end %>
  <div class="form">
    <% @room.chat_messages.each do |message| %>
      <div class="messages">
        <p>内容：<%= message.content %></p>
        <p>名前：<%= message.user.name %></p>
        <p>時刻：<%= message.created_at %></p>
      </div>
    <% end %>
  </div>
  <div id="add-message">
  a
  </div>
</div>  