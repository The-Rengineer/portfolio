<div class="main posts-new">
  <p><%= "ルーム#{@room.id}"%></p>
  <%# <%= form_tag("/chat_messages/create/#{@room.id}") do %>
    <div class="form">
      <div class="form-body">
      <form>
        <input type="text" id="content" data-behavior="room_speaker_keypress" data-user=<%= @current_user.id%> data-room=<%= @room.id%>>
        <input type="submit" id ="save" data-behavior="room_speaker_click" value="保存">
      </form>
      </div>
    </div>
  <%# <% end %>
  <div id="form2">
    <% @chats.each do |message| %>
      <div class="messages">
        <p>内容：<%= message.content %></p>
        <p>名前：<%= message.user.name %></p>
        <p>時刻：<%= message.created_at.strftime("%Y年%m月%d日 %H:%M:%S") %></p>
      </div>
    <% end %>
  </div>
</div>  